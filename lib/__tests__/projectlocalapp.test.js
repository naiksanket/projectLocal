const assert = require('assert');
const projectlocalapp = require('../index.js');

describe('projectlocalapp', () => {
  it('has a test', () => {
    assert(false, 'projectlocalapp should have a test');
  });
});
